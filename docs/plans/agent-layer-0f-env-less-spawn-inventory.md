# Agent-layer 0f: env-less spawn inventory + isolation decision

Date: 2026-08-24.  Tree: `acb991e05` (`dev`).

This is the reviewable artifact for the 0f slice of the superpipe pilot (§8.1 of `docs/agent-layer-superpipe-pilot-proposal.md`).  It identifies every daemon process launch that currently inherits the live `process.env`, maps each to one of the three isolation treatments, and records the design decisions that 0g will mechanically apply.

## Context

The 0a–0e slices build or enroll the `provider-env-coordinator` and the QueryRunner/ACP/ProviderService readers so that `process.env` is no longer mutated while a session holds a lease.  The remaining risk is that **any child process launched without an explicit `env` option continues to inherit the live `process.env`**.  During a lease window that can include another session's API keys, tokens, or routing variables, so every such launch must be either enrolled in the coordinator, sanitized with an explicit allowlist, or moved to an immutable baseline snapshot.

## Method

A source sweep of `packages/daemon/src` for `node:child_process` and `Bun.spawn` calls was performed.  A call was marked *env-less* when its options object contained no `env:` property and the spawn implementation falls back to `process.env` (or when the env was built from `process.env` without an explicit allowlist).  Calls that already pass a constructed or allowlisted env are recorded as *already protected / verification only*.  The anchors below are current (`acb991e05`) line numbers.

## 1. Direct env-inheriting launches

| Source | Line(s) | Command / purpose | Current env handling | Decision | Notes |
|---|---|---|---|---|---|
| `packages/daemon/src/lib/runtime-spawn/bun-backend.ts` | 17 | `Bun.spawn(args, options \|\| {})` | `env` omitted ⇒ `process.env` inherited | **sanitize** | Root `spawnProcess` default.  The Bun backend must require an explicit `env` or default to a safe coordinator-supplied baseline instead of the live process. |
| `packages/daemon/src/lib/runtime-spawn/node-backend.ts` | 37 | `spawn(args[0], args.slice(1), { ... })` | `env: options?.env` ⇒ `process.env` inherited when `env` is absent | **sanitize** | Node `spawn` fallback is identical to the Bun case; the `SpawnFn` contract must not allow an undefined env to leak live `process.env`. |
| `packages/daemon/src/lib/space/runtime/workflow-executor.ts` | 33–37, 127–137 | `sh -c <user condition expression>` | `spawnProcess(args, { cwd, ... })` with no `env` | **sanitize** | Condition expressions are arbitrary shell and must not see session API keys. 0g passes a fixed, credential-free workflow baseline (PATH, HOME, TMPDIR, etc.).  If a condition needs non-standard variables, the workflow schema may declare them in a new `condition.allowedEnv` field, but 0g rejects known credential/routing names and injects values only from an immutable, credential-free baseline or a workflow-scoped immutable value source, never from live `process.env`. |
| `packages/daemon/src/lib/rpc-handlers/dialog-handlers.ts` | 22, 30, 37, 57, 70, 145 | `osascript`/`zenity`/`kdialog`/`powershell`/`which` | `Bun.spawn([cmd, ...args], { stdout: 'pipe', stderr: 'pipe' })` with no `env` | **sanitize** | The safe desktop baseline must include GUI-session variables (`DISPLAY`, `WAYLAND_DISPLAY`, `XDG_RUNTIME_DIR`, `DBUS_SESSION_BUS_ADDRESS`, `XAUTHORITY` and their Windows analogs) so `zenity`/`kdialog` can connect, while still excluding provider credentials and routing variables. |
| `packages/daemon/src/lib/agent/sdk-cli-resolver.ts` | 205, 228, 382 | `curl` npm metadata, `curl` tarball, `which rg` | `execFileSync`/`execSync` with no `env` | **sanitize** | Build a purpose-specific `buildCurlEnv` from the immutable startup baseline that includes both uppercase and lowercase proxy variables (`HTTPS_PROXY`, `https_proxy`, `HTTP_PROXY`, `http_proxy`, `ALL_PROXY`, `all_proxy`, `NO_PROXY`, `no_proxy`) and TLS/CA variables (`CURL_CA_BUNDLE`, `SSL_CERT_FILE`, `SSL_CERT_DIR`); do not include live session credentials or routing variables. |
| `packages/daemon/src/lib/providers/anthropic-copilot/provider.ts` | 673 | `gh auth token` | `execFileAsync('gh', ['auth', 'token'], { timeout: 5000 })` with no `env` | **sanitize** | This must run with a GitHub allowlist built from the provider's constructor `env` / `credentialEnvBaseline`, not the live `process.env`. |
| `packages/daemon/src/lib/worktree-manager.ts` | 544 | `gh` JSON calls | `execFile('gh', args, { cwd, encoding, timeout, maxBuffer }, ...)` with no `env` | **sanitize** | Use a platform-aware `buildGitHubLookupEnv` allowlist (GitHub tokens, GH_HOST, PATH, HOME, XDG, plus Windows runtime keys) bound to the current owner's baseline. |
| `packages/daemon/src/lib/space/managers/space-worktree-manager.ts` | 62, 72, 79, 86, 101, 160, 172, 235, 250 | `git worktree` / `git branch` | `execFileSync('git', ...)` with no `env` (10 sites) | **sanitize** | A platform-aware `buildGitCommandEnv` allowlist of safe, non-secret Git variables (PATH, HOME, USER, XDG_CONFIG_HOME, `GIT_SSL_CAINFO`, `GIT_SSL_NO_VERIFY`, `GIT_EXEC_PATH`, etc., plus Windows runtime keys).  It must explicitly exclude `GIT_CONFIG_COUNT`, `GIT_CONFIG_KEY_*`, `GIT_CONFIG_VALUE_*`, `GIT_SSH_COMMAND`, `GIT_ASKPASS`, `SSH_ASKPASS`, and any other env that can carry `http.extraHeader`, credential-bearing-SSH, or askpass secrets.  `SSH_AUTH_SOCK`, `GIT_SSH_COMMAND`, and askpass inputs are **not** included in the common Git env because `git worktree add` can run a `post-checkout` hook; they are added only for the `git submodule update` call at `worktree-manager.ts:742` via a separate `buildGitSshEnv` from the owner baseline. |
| `packages/daemon/src/lib/space/artifact-git-ops.ts` | 35 | `git` diff / log / remote | `execFile('git', args, { cwd, encoding, timeout, maxBuffer }, ...)` with no `env` | **sanitize** | Same safe `buildGitCommandEnv` (non-secret keys only, excluding `GIT_CONFIG_*` secret carriers). |
| `packages/daemon/src/lib/space/managers/space-manager.ts` | 275 | `git rev-parse --git-dir` | `execAsync('git ...', { cwd })` with no `env` | **sanitize** | Same safe `buildGitCommandEnv` (non-secret keys only, excluding `GIT_CONFIG_*` secret carriers). |
| `packages/daemon/src/lib/worktree-manager.ts` | 60, 738, 742, 758, 919 and `getGit` uses | `simple-git` instances | `simpleGit(repoPath)` / `simpleGit(worktreePath)` with no `env`; all underlying `git` child processes inherit `process.env` | **sanitize** | 0g configures the default `simple-git` instance with the safe `buildGitCommandEnv` via its constructor `env` so its `git.raw(...)`, `git.branch(...)`, etc. do not inherit live `process.env`.  `buildGitCommandEnv` must **not** include `SSH_AUTH_SOCK`, `GIT_SSH`, `GIT_SSH_COMMAND`, or askpass variables by default, because `git worktree add` at `worktree-manager.ts:738` can run a `post-checkout` hook.  The `git submodule update --init --recursive` call at `worktree-manager.ts:742` must use a dedicated one-shot `simpleGit(worktreePath, { env: buildGitSshEnv(...) })` instance (do not reuse the cached instance and do not use `.env()` as a per-call override, because `SimpleGit.env()` is instance-level) that adds `SSH_AUTH_SOCK`, `GIT_SSH`, `GIT_SSH_COMMAND`, and askpass inputs (`GIT_ASKPASS`, `SSH_ASKPASS`, and any helper-specific variables) from the owner baseline so private SSH submodules can authenticate. |
| `packages/daemon/src/lib/process-watchdog.ts` | 35, 41 | `ps` listing | `execFileAsync('ps', ...)` with no `env` | **sanitize** | `ps` needs PATH only (or an empty env with an absolute path). |
| `packages/daemon/src/lib/credential-discovery.ts` | 66 | `security find-generic-password` | `execSync('security ...')` with no `env` | **sanitize** | macOS Keychain call; minimal baseline.  This file also writes `process.env` (lines 48, 76, 92), which is an ambient-writer concern tracked under 0e. |
| `packages/daemon/src/lib/credentials/credential-store.ts` | 38, 73, 94, 152, 164, 416 | `security` CRUD and `security unlock-keychain` | `execFile` / `spawn` / `execFileAsync` with no `env` | **sanitize** | Keychain CLI calls need only PATH/HOME.  The `DatabaseCredentialStore` `ENCRYPTION_KEY_ENV` reader at 448–449 is an ambient reader handled under 0e. |

## 2. Spawner seams and default `spawnImpl`s

| Source | Line(s) | Purpose | Current env handling | Decision | Notes |
|---|---|---|---|---|---|
| `packages/daemon/src/lib/space/runtime/connectors/github-connector.ts` | 17 | `createGithubConnector(spawnImpl = spawnProcess)` | Default `spawnImpl` is the raw `spawnProcess`, but all call sites use `runGhJson` which passes `buildGitHubLookupEnv()` | **enroll / sanitize** | The default must either be enrolled in the coordinator or require an explicit env.  Because the connector is always invoked through `runGhJson`, the immediate fix is to keep `buildGitHubLookupEnv()` and verify every call path supplies it.  `buildGitHubLookupEnv()` itself must be rewritten to source its values from the connector's owner baseline (§2 `gh-lookup-helpers.ts`), not from live `process.env`. |
| `packages/daemon/src/lib/space/runtime/connectors/presets.ts` | 18, 59, 77, 141, 160 | Validator factories default to `spawnProcess` | Same as github-connector; helpers pass `buildGitHubLookupEnv()` | **enroll / sanitize** | Same treatment: default `spawnImpl` must not be able to launch with live `process.env`. |
| `packages/daemon/src/lib/space/runtime/connectors/production.ts` | 23 | `registerProductionConnectors(spawnImpl = spawnProcess)` | Same as github-connector | **enroll / sanitize** | Same. |
| `packages/daemon/src/lib/space/runtime/built-in-validators/pr-ready-validator.ts` | 58, 116, 135, 188 | `spawnImpl` default and calls | `runTextCommand` and `runCommand` pass `buildGitHubLookupEnv()`; the `git config` call (279) also uses that allowlist | **sanitize** | Continue to pass an explicit allowlist.  The `git config` call does not need GH tokens, so a generic `buildCommandEnv` is cleaner, but the GitHub allowlist is safe.  The allowlist must be sourced from the validator's owner baseline once `buildGitHubLookupEnv()` is rewritten (§2 `gh-lookup-helpers.ts`). |
| `packages/daemon/src/lib/space/runtime/hook-executor.ts` | 302 | Hook script `bash -c` execution | `spawnProcess(args, { cwd, env: restrictedEnv, ... })` | **sanitize (source fix in 0g)** | `buildHookRestrictedEnv` currently sources from `Object.entries(process.env)` and must be updated in 0g to use the hook owner's immutable env or the active coordinator baseline only when the hook carries the matching lease token; otherwise it falls back to a credential-free baseline. |
| `packages/daemon/src/lib/space/runtime/gh-lookup-helpers.ts` | 49–56, 58–72 | `buildGitHubLookupEnv()`, `resolveGithubConfigDir()` | Read `GH_TOKEN`, `GITHUB_TOKEN`, `GH_ENTERPRISE_TOKEN`, `GITHUB_ENTERPRISE_TOKEN`, `GH_HOST`, `GH_CONFIG_DIR`, and `XDG_CONFIG_HOME` directly from `process.env` | **sanitize** | 0g rewrites both helpers to accept an immutable owner baseline or a matching lease token. `buildGitHubLookupEnv(base)` copies the allowed keys from that baseline, and `resolveGithubConfigDir(base)` resolves `GH_CONFIG_DIR` / `XDG_CONFIG_HOME` from the same baseline. Callers (`github-connector.ts:22`, `pr-ready-validator.ts:311/490`, `hook-executor.ts:149`) must pass the correct baseline. |

## 3. SDK-hosted spawns (indirect, via SDK `query` or ACP client)

| Source | Line(s) | Purpose | Current env handling | Decision | Notes |
|---|---|---|---|---|---|
| `packages/daemon/src/lib/github/security-agent.ts` | 162–193 | `query()` API key hand-off | Sets `process.env.ANTHROPIC_API_KEY` / `CLAUDE_CODE_OAUTH_TOKEN` before `query()`, restores after | **sanitize** | SDK `query` accepts an `env` option.  Construct an explicit `env` from the agent's immutable key baseline plus a safe PATH/HOME baseline, proxy/TLS inputs (`HTTPS_PROXY`, `https_proxy`, `HTTP_PROXY`, `http_proxy`, `NO_PROXY`, `no_proxy`, `NODE_USE_ENV_PROXY`, `NODE_EXTRA_CA_CERTS`), and routing inputs (`ANTHROPIC_BASE_URL`); do not mutate `process.env`. |
| `packages/daemon/src/lib/github/router-agent.ts` | 202–233 | `query()` API key hand-off | Same set/restore pattern | **sanitize** | Same treatment as security-agent, including proxy/TLS (`HTTPS_PROXY`, `https_proxy`, `HTTP_PROXY`, `http_proxy`, `NO_PROXY`, `no_proxy`, `NODE_USE_ENV_PROXY`, `NODE_EXTRA_CA_CERTS`) and `ANTHROPIC_BASE_URL` routing inputs. |
| `packages/daemon/src/lib/providers/anthropic-provider.ts` | 169–185 | `loadModelsFromSdk()` model discovery | `applyEnvVarsForSdk` sets/restores `process.env` around SDK `query`, which is called without `env` | **enroll / sanitize** | `anthropic.loadModelsFromSdk` is already a coordinator owner.  0g either wraps the call in `providerEnvCoordinator.runWithLease` or, preferably, passes explicit `env` built from the provider's immutable baseline and removes the `process.env` mutation. |
| `packages/daemon/src/lib/providers/anthropic-copilot/provider.ts` | 903–906 | Copilot client start | `const env = { ...this.env }` followed by `new CopilotClient({ useStdio: true, env })` where `this.env` defaults to live `process.env` | **sanitize** | 0g builds the Copilot client env from a credential-free OS baseline, plus `COPILOT_GITHUB_TOKEN` / `GH_TOKEN`, `GITHUB_API_URL`, and `GITHUB_COPILOT_CLIENT_ID` from the provider's immutable baseline, plus a purpose-specific proxy/TLS allowlist (`HTTPS_PROXY`, `NO_PROXY`, `NODE_EXTRA_CA_CERTS`, etc.) sourced from that same baseline; it must not spread live `process.env`.  After building the sanitized env, overlay the resolved `token` as `COPILOT_GITHUB_TOKEN` so the client is authenticated for `gh auth token` / `hosts.yml` login paths.  `GITHUB_API_URL` is required so the stdio client continues to route to GitHub Enterprise (`provider.ts:692-702`).
| `packages/daemon/src/lib/acp/acp-model-fetcher.ts` | 27, 65 | ACP session disposal and model discovery | `disposeAcpSessions` passes no `env`; `fetchAcpModels` uses `buildAcpDiscoveryEnv()` (alias of `buildAcpSafeEnv`) with `replaceEnv: true`, which strips credentials | **sanitize** | `disposeAcpSessions` calls `initialize()` and `authenticate()`, so it needs a credential-bearing ACP env associated with the provider/session (e.g., the same `acpEnv` used by the query runner).  `fetchAcpModels` is invoked from RPC without a session credential baseline; it must stay on `buildAcpDiscoveryEnv()` with `replaceEnv: true` so the discovery executable does not receive arbitrary provider or session secrets.  `acp-provider.ts:28` `defaultAcpCommandProbe` only calls `initialize()` and must remain on the safe replacement environment to avoid exposing tokens to an availability check. |
| `packages/daemon/src/lib/acp/acp-query-runner.ts` | 622–633 | ACP query process launch | `new AcpClient({ command, args, env: acpEnv })` with default `replaceEnv: false` | **sanitize** | `acpEnv` must be built from a safe runtime baseline plus only the selected ACP provider's credentials, and the `AcpClient` must be constructed with `replaceEnv: true` so `buildAcpProcessEnv()` does not start from `{ ...process.env }` (`acp-transport.ts:26`).  `acp-transport.ts:88` is only safe when `replaceEnv` is explicitly true. |

## 4. Already explicit / enrolled / verification-only paths

| Source | Line(s) | Purpose | Current env handling | Decision | Notes |
|---|---|---|---|---|---|
| `packages/daemon/src/lib/agent/query-options-builder.ts` | 323, 361, 769–776 | Main SDK query environment builder | `getMergedEnvironmentVars()` builds `mergedEnv` from `Object.entries(process.env)` filtered by a denylist (`PORT`, `HYPERNEO_PORT`, `NEOKAI_PORT`, `processProviderEnvVars`) and adds selected provider/session variables | **sanitize** | This is the central environment used by `query-runner.ts`.  0g must build `mergedEnv` from a safe, credential-free runtime baseline plus only the selected provider's credentials and explicitly declared session variables, instead of starting from the full `process.env` and filtering.  The exclusions may remain as a denylist, but the source baseline must not contain unrelated session secrets. |
| `packages/daemon/src/lib/agent/query-runner.ts` | 42–53 | SDK `nodeSpawn` default | `env: opts.env` | **verify** | Receives a constructed `options.env` from `QueryOptionsBuilder` and passes it to the SDK child.  0g verifies the builder no longer spreads live `process.env`; the launch itself is already explicit. |
| `packages/daemon/src/lib/session/session-lifecycle.ts` | 957 | Title generation query | `env: mergedEnv` from `buildSdkQueryEnv` | **sanitize** | SDK `query` is called with an explicit `env`, but `buildSdkQueryEnv` uses `mergeProviderEnvVars`, which returns `{ ...process.env, ...providerEnvVars }` (`provider-service.ts:828-830`).  0g must rewrite `mergeProviderEnvVars` to start from a safe, credential-free runtime baseline plus only the selected provider's credentials, and `buildSdkQueryEnv` must not spread live `process.env`. |
| `packages/daemon/src/lib/agent/limit-error-llm-classifier.ts` | 267 | Limit-error classification query | `env: mergeProviderEnvVars(providerEnvVars)` | **sanitize** | Same as `session-lifecycle.ts:957`: the `env` is explicit but `mergeProviderEnvVars` starts from `{ ...process.env }` (`provider-service.ts:828-830`); 0g rewrites it to a safe runtime baseline plus provider vars. |
| `packages/daemon/src/lib/space/evolution-episode-service.ts` | 761 | Episode-judge query | `env: mergeProviderEnvVars(providerEnvVars)` | **sanitize** | Same as `session-lifecycle.ts:957`: `mergeProviderEnvVars` starts from `{ ...process.env }` (`provider-service.ts:828-830`); 0g rewrites it to a safe runtime baseline plus provider vars. |
| `packages/daemon/src/lib/space/evolution-conversation-analysis-service.ts` | 296 | Conversation-friction query | `env: mergeProviderEnvVars(providerEnvVars)` | **sanitize** | Same as `session-lifecycle.ts:957`: the `env` is explicit but `mergeProviderEnvVars` starts from `{ ...process.env }` (`provider-service.ts:828-830`); 0g rewrites it to a safe runtime baseline plus provider vars. |
| `packages/daemon/src/lib/space/runtime/llm-workflow-selector.ts` | 61 | Workflow-selection query | `env: mergedEnv` from `mergeProviderEnvVars` | **sanitize** | Same as `session-lifecycle.ts:957`: the `env` is explicit but `mergeProviderEnvVars` starts from `{ ...process.env }` (`provider-service.ts:828-830`); 0g rewrites it to a safe runtime baseline plus provider vars. |
| `packages/daemon/src/lib/acp/acp-transport.ts` | 88 | ACP process spawn | `env: buildAcpProcessEnv(env, replaceEnv)` | **verify** | The helper is correct when `replaceEnv` is `true` and a constructed `env` is passed.  It is unsafe when `replaceEnv` is `false` because it starts from `{ ...process.env }`; callers must avoid the default. |
| `packages/daemon/src/lib/acp/acp-terminal-manager.ts` | 71 | ACP interactive terminal | `env: this.baseEnv` (default `{}`, supplied as `buildAcpSafeEnv(acpEnv)` in call site) | **verify** | Call site constructs the env; terminal spawn itself is explicit.  0g verifies `baseEnv` is never left as the literal empty `{}` when a terminal starts. |


## 5. Cross-referenced ambient env writers and readers (not launches, but they feed launches)

These are not spawns, but they explain why live `process.env` is dangerous.  They are tracked in the parallel 0a–0e slices, not fixed by 0f/0g.

| Source | Line(s) | Behavior | Treatment (pre-decided) |
|---|---|---|---|
| `packages/daemon/src/lib/credential-discovery.ts` | 48, 76, 92 | Writes credentials into `process.env` | Enroll in provider-env coordinator (0e scope) |
| `packages/daemon/src/lib/github/security-agent.ts` | 162–193 | Mutates `process.env` around SDK call | Remove by using SDK `env` option (this doc) |
| `packages/daemon/src/lib/github/router-agent.ts` | 202–233 | Mutates `process.env` around SDK call | Remove by using SDK `env` option (this doc) |
| `packages/daemon/src/lib/credentials/credential-store.ts` | 448–449 | Reads `process.env[ENCRYPTION_KEY_ENV]` | Move to immutable baseline (0e scope) |
| `packages/daemon/src/lib/providers/anthropic-provider.ts` | 342–370 | `applyEnvVarsForSdk` sets/restores `process.env` around SDK `query` | Pass explicit `env` to SDK `query` and remove `process.env` mutation (0g) |
| `packages/daemon/src/lib/space/runtime/connectors/github-connector.ts` | 49, 177 | Reads `process.env.GH_HOST` for PR host validation | Move to connector context / owner baseline (0g) |
| `packages/daemon/src/lib/space/runtime/built-in-validators/pr-ready-validator.ts` | 274-277 | Reads `process.env.GH_HOST` / `GH_REPO` for GitHub host inference | Move to validator context / owner baseline (0g) |

## Decision

1. **Default rule:** every new process launch in the daemon must receive an explicit `env`.  The `runtime-spawn` `SpawnFn` contract will be updated so that an omitted `env` does **not** fall back to live `process.env`.  If the caller propagates a valid `provider-env-coordinator` lease token, the fallback is the coordinator-managed baseline for that token.  Otherwise the fallback is a **credential-free** OS/runtime allowlist (PATH, HOME, USER, locale, XDG, and Windows runtime keys), not the unfiltered startup snapshot.  This makes the root spawn implementation **sanitize-by-default** and prevents both background and startup credential leaks.

2. **Sanitize** is the preferred treatment for the bulk of the sites: `git`, `gh`, `security`, `ps`, `curl`, `which`, `osascript`/`zenity`/`kdialog`/`powershell`, the bun-node-sqlite probe, user condition expressions, and the GitHub CLI token reader.  Each gets a small, purpose-built allowlist derived from the current owner's immutable baseline (or the coordinator's active lease for session-owned calls).  No site in this inventory is allowed to pass the full live `process.env` to a child.

3. **Enroll** is used only for the `runtime-spawn` / connector `spawnImpl` seam so that session-owned code paths can continue to receive the coordinator-managed env.  Enrolled sites still pass an explicit `env`; they do not inherit `process.env`.

4. **Immutable baseline** is the rule for all credential and API-key reads.  Providers, agents, and credential readers must capture the env they need at construction (or per-owner initialization) and stop reading `process.env` after that point.  The Copilot provider's `credentialEnvBaseline` is the existing pattern; the GitHub router/security agents and the ACP terminal manager should adopt it.

5. **0g mechanical application** will: (a) add `env` to every call in §1 using a platform-aware `build*Env` helper (e.g., `buildCurlEnv` for `sdk-cli-resolver.ts` must include both uppercase and lowercase proxy variables and TLS/CA variables `CURL_CA_BUNDLE`, `SSL_CERT_FILE`, and `SSL_CERT_DIR`); (b) update `runtime-spawn` to require a propagated lease token before using the active coordinator baseline, and otherwise fall back to a credential-free OS/runtime allowlist; (c) fix `acp-model-fetcher.ts:27` (`disposeAcpSessions`) with a credential-bearing ACP env and `replaceEnv: true`, keep `acp-model-fetcher.ts:65` (`fetchAcpModels`) on the safe `buildAcpDiscoveryEnv()` with `replaceEnv: true` because it is invoked from RPC without a session credential baseline, and keep `acp-provider.ts:28` `defaultAcpCommandProbe` on the safe replacement environment because it only calls `initialize()`; also set `acp-query-runner.ts:622` to use `replaceEnv: true` with a constructed `acpEnv`; (d) rewrite `mergeProviderEnvVars` to start from a safe, credential-free runtime baseline plus only the selected provider's credentials, and update every caller in §4 (`session-lifecycle.ts`, `limit-error-llm-classifier.ts`, `evolution-episode-service.ts`, `evolution-conversation-analysis-service.ts`, `llm-workflow-selector.ts`) so their explicit `env` no longer spreads live `process.env`; also rewrite `QueryOptionsBuilder.getMergedEnvironmentVars()` so the main SDK query environment is constructed from the same safe runtime baseline plus the selected provider's credentials and explicitly declared session variables, instead of starting from `Object.entries(process.env)` and filtering; (e) remove the `process.env` set/restore blocks in the GitHub agents and `anthropic-provider.ts:169-185` by using the SDK `env` option and including proxy/TLS inputs (`HTTPS_PROXY`, `https_proxy`, `HTTP_PROXY`, `http_proxy`, `NO_PROXY`, `no_proxy`, `NODE_USE_ENV_PROXY`, `NODE_EXTRA_CA_CERTS`) and routing inputs (`ANTHROPIC_BASE_URL`); (f) build `anthropic-copilot/provider.ts:903-906` Copilot client env from a credential-free baseline, the needed tokens (`COPILOT_GITHUB_TOKEN` / `GH_TOKEN`), `GITHUB_API_URL`, `GITHUB_COPILOT_CLIENT_ID`, and a proxy/TLS allowlist sourced from the provider's immutable baseline, and overlay the resolved `token` as `COPILOT_GITHUB_TOKEN` before `client.start()`; (g) update `hook-executor.ts:153-191` (`buildHookRestrictedEnv`) and line 149 (`resolveGithubConfigDir()`) to use the hook owner’s immutable env or the active coordinator baseline only when the matching lease token is present, so `GH_CONFIG_DIR` is never resolved from live `process.env`; (h) add `condition.allowedEnv` to the workflow schema and runner, with a non-bypassable credential/routing-name filter and values sourced from an immutable baseline, never live `process.env`; (i) configure the default `simple-git` instance in `worktree-manager.ts` with the sanitized `buildGitCommandEnv` via its constructor `env`; (j) define `buildGitCommandEnv` as an explicit allowlist of safe, non-secret Git variables and explicitly exclude `GIT_CONFIG_COUNT`, `GIT_CONFIG_KEY_*`, `GIT_CONFIG_VALUE_*`, `SSH_AUTH_SOCK`, `GIT_SSH`, `GIT_SSH_COMMAND`, `GIT_ASKPASS`, `SSH_ASKPASS`, and other `http.extraHeader`/secret carriers; add a separate `buildGitSshEnv` and invoke the `git submodule update` call at `worktree-manager.ts:742` through a dedicated one-shot `simpleGit(worktreePath, { env: buildGitSshEnv(...) })` instance (do not reuse the cached instance and do not use `.env()` as a per-call override, because `SimpleGit.env()` is instance-level) that adds `SSH_AUTH_SOCK`, `GIT_SSH`, `GIT_SSH_COMMAND`, and askpass inputs (`GIT_ASKPASS`, `SSH_ASKPASS`, and any helper-specific variables) from the owner baseline; (k) rewrite `gh-lookup-helpers.ts:49-72` so `buildGitHubLookupEnv()` and `resolveGithubConfigDir()` accept an owner baseline or matching lease token and stop reading live `process.env`; (l) move the `process.env.GH_HOST` / `GH_REPO` reads in `github-connector.ts:49,177` and `pr-ready-validator.ts:274-277` to their owner baselines.

This decision makes the daemon's child processes independent of the live `process.env` mutation window, closing the cross-session credential-leak path described in §8.1.
