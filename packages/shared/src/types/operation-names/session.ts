export const SESSION_OPERATION_NAMES = [
  'session.clone.spawn',
  'session.get',
  'session.interrupt',
  'session.list',
  'session.message.list',
  'session.message.send',
  'session.state.update',
] as const;
