# Node
node_modules/
package-lock.json

# Environment
.env
.env.local

# IDE
.vscode/

# CI Artifacts (downloaded test results)
e2e-no-llm-results-*/
e2e-no-llm-results-*
e2e-results-llm-*/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Build outputs
dist/
build/
_fresh/

# Generated embedded assets (created during binary build)
packages/cli/src/embedded-assets.ts

# Logs
*.log
logs/

# Database
*.db
*.sqlite
*.sqlite3

# Temp files
temp/
tmp/
coverage/
data/
test-results/
playwright-report/
*.tsbuildinfo

# Git worktrees
.worktrees/
**/settings.local.json
# Coverage output
coverage/
coverage-merged/
coverage-parts/
**/coverage/
browser-coverage.json
browser-coverage.lcov

# Dev Proxy runtime files
.devproxy/.devproxy.pid
.devproxy/devproxy.log

# Desktop (Tauri) build artifacts
packages/desktop/src-tauri/target/
packages/desktop/src-tauri/binaries/
packages/desktop/src-tauri/gen/

# Claude Code — ignore everything under .claude/
.claude/
# Local tool artifacts (codebase-memory index, Playwright CLI cache)
.codebase-memory/
.playwright-cli/
packages/daemon/.embedded-sdk-cli

# Local research artifacts
research-papers/
.codegraph/
graphify-out/

# Local scratch/debug output (review-round captures, e.g. r4d.out, r12b.out)
/r*.out

# Deno — managed by `deno install` during the `deno compile` smoke flow;
# reproducible enough to regenerate, and the smoke binary is not on the release
# pipeline (Bun stays the pinned release runtime).
deno.lock
